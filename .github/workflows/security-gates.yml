name: Security Gates 🛡️

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Validate Workflow
        run: echo "Validation successful"

  security-gates:
    uses: Trendyol/security-actions/.github/workflows/security-gates.yml@e055939b4c4de528d7a4ecf9262baa3cf1db4b88 #v1.0.4
    permissions:
      actions: read
      contents: read
      security-events: write
    secrets: inherit
