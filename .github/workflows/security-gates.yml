name: Security Gates 🛡️

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Validate Workflow
        run: echo "Validation successful"

  security-gates:
    uses: Trendyol/security-actions/.github/workflows/security-gates.yml@e553e38ace4f3a3fdfb6336d76c5f8a069b0aee8
    permissions:
      actions: read
      contents: read
      security-events: write
    secrets: inherit
